/* import type { QuestionViewModel } from '@/domains/survey/createSurvey.usecase'

export function formHaveCharacters(surveyName: string, nextQuestion: QuestionViewModel): boolean {
  return (
    surveyName.length > 0 &&
    nextQuestion.questionName.length > 0 &&
    nextQuestion.answerOne.length > 0 &&
    nextQuestion.answerTwo.length > 0 &&
    nextQuestion.answerThree.length > 0 &&
    nextQuestion.answerFour.length > 0
  )
}
 */
